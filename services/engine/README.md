# zipline-engine

Python service for ZIPLINE: strategies (zipline-reloaded lineage), treasury accounting, netting,
execution adapters, reconciliation, FastAPI, scheduler. See the repository README for everything.
